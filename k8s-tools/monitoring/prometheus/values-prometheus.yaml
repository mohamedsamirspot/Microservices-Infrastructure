server:
  persistentVolume:
    enabled: false
  retention: 10d
  global:
    scrape_interval: 1m

serverFiles:
  prometheus.yml:
    global:
      external_labels:
        cluster: my-cluster-name

prometheus-pushgateway:
  enabled: false

alertmanager:
  persistence:
    enabled: false

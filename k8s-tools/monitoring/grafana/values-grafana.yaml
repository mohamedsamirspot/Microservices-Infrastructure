persistence:
  enabled: false

# adminUser: admin
# adminPassword: "${grafanaadminpassword}"

admin:
  existingSecret: "grafana-admin-credentials"
  userKey: userKey
  passwordKey: passwordKey

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi


# cd /etc/grafana/datasources
datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      uid: bdvw8iqhzrm68d
      type: prometheus
      url: http://prometheus-server.monitoring.svc.cluster.local
      access: proxy
      isDefault: true
      editable: true

# cd /etc/grafana/provisioning/dashboards --> here you just define the place where the dashboards are located
## Configure grafana dashboard to import
## NOTE: To use dashboards you must also enable/configure dashboardProviders
## ref: https://grafana.com/dashboards
##
## dashboards per provider, use provider name as key.
# take care these configs will create an empty directory called default under /var/lib/grafana/dashboards/ anyway
dashboardProviders:
  dashboardproviders.yaml:
    apiVersion: 1
    providers:
      - name: general
        orgId: 1
        folder: ""                # This puts dashboards in the 'General' folder in Grafana UI
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/  # dashboards go directly here

      - name: kubernetes
        orgId: 1
        folder: "kubernetes"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/kubernetes

# Define dashboards to be imported
dashboards:
  general:
    blackbox-exporter:
      url: https://raw.githubusercontent.com/mohamedsamirspot/Microservices-Infrastructure/refs/heads/test/k8s-tools/monitoring/grafana/dashboards/blackbox-exporter.json
  kubernetes:
    k8s-pods:
      url: https://raw.githubusercontent.com/mohamedsamirspot/Microservices-Infrastructure/refs/heads/test/k8s-tools/monitoring/grafana/dashboards/k8s-pods.json

# cd /var/lib/grafana/plugins --> here you define the plugins to be installed
plugins:
  - grafana-github-datasource

# ==================== ALERTING CONFIGURATION ====================

# smtp:
#   enabled: true
#   host: smtp.gmail.com:587
#   existingSecret: "grafana-smtp-secret"
#   userKey: user
#   passwordKey: password
#   fromAddress: mohamed.samir.emary@gmail.com
#   fromName: Grafana
#   startTLSPolicy: "MandatoryStartTLS"
smtp:
  enabled: true
  host: smtp.gmail.com:587
  user: mohamed.samir.emary@gmail.com
  password: "urzxrxlwneyiapcw"  # Use App Password, not your regular Gmail password
  fromAddress: mohamed.samir.emary@gmail.com
  fromName: Grafana
  startTLSPolicy: "MandatoryStartTLS"

alerting:
  contactpoints.yaml:
    url: https://raw.githubusercontent.com/mohamedsamirspot/Microservices-Infrastructure/refs/heads/test/k8s-tools/monitoring/grafana/alerting/contactpoints.yaml
  policies.yaml:
    url: https://raw.githubusercontent.com/mohamedsamirspot/Microservices-Infrastructure/refs/heads/test/k8s-tools/monitoring/grafana/alerting/policies.yaml
  rules.yaml:
    url: https://raw.githubusercontent.com/mohamedsamirspot/Microservices-Infrastructure/refs/heads/test/k8s-tools/monitoring/grafana/alerting/rules.yaml

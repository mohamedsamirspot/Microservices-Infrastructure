persistence:
  enabled: false

# adminUser: admin
# adminPassword: "${grafanaadminpassword}"

admin:
  existingSecret: "grafana-admin-credentials"
  userKey: userKey
  passwordKey: passwordKey

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

# Disable password leak detection (allows inline passwords)
# assertNoLeakedSecrets: false
# grafana.ini:
#   smtp:
#     enabled: true
#     host: smtp.gmail.com:587
#     user: mohamed.samir.emary@gmail.com
#     password: urzxrxlwneyiapcw
#     from_address: mohamed.samir.emary@gmail.com
#     from_name: Grafana Alerts
#     startTLS_policy: MandatoryStartTLS
#     skip_verify: false

# ==================== SIDECAR CONFIGURATION ====================
# All datasources, dashboards, plugins, and alerts are managed via ConfigMaps
sidecar:
  # cd /etc/grafana/provisioning/datasources
  datasources:
    enabled: true
    label: grafana_datasource
    labelValue: "1"
    searchNamespace: monitoring
    reloadURL: "http://localhost:3000/api/admin/provisioning/datasources/reload"
  
  # cd /etc/grafana/provisioning/dashboards --> here you just define the place where the dashboards are located
  dashboards:
    enabled: true
    label: grafana_dashboard
    labelValue: "1"
    folder: /var/lib/grafana/dashboards
    searchNamespace: monitoring
    folderAnnotation: grafana_folder
    provider:
      foldersFromFilesStructure: true
  
  # cd /etc/grafana/provisioning/alerting
  alerts:
    enabled: true
    label: grafana_alert
    labelValue: "1"
    folder: /etc/grafana/provisioning/alerting
    searchNamespace: monitoring
    reloadURL: "http://localhost:3000/api/admin/provisioning/alerting/reload"

  # cd /var/lib/grafana/plugins --> here you define the plugins to be installed
  plugins:
    enabled: true
    label: grafana_plugin
    labelValue: "1"
    searchNamespace: monitoring
    initPlugins: true  # Install before Grafana starts
